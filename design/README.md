# design/

The project brief and forward-looking design notes.

- `brief.md`: the requirements the project was built against.
- `portability.md`: how the task engine could run on other workflow engines (Cloudflare Workflows, Restate, Temporal) and what the public API locks down to keep that cheap.

Finished documentation lives in `docs/`.
