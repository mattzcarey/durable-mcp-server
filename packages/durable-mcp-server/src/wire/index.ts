/**
 * MCP Tasks extension wire module: the only import source for task wire types
 * and schemas in this repo (docs/how-it-works.md §7 (the wire contract served) — the SDK's deprecated 2025-11-25
 * task exports are banned).
 */

export * from "./schemas";
export * from "./types";
